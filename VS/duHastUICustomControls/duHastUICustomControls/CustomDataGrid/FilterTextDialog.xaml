<Window x:Class="duHastNet.UI.CustomControls.CustomDataGrid.FilterTextDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Filter Column"
        Width="450"
        Height="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <!-- Dialog styling -->
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="BorderBrush" Value="#2196F3"/>
                </Trigger>
                <Trigger Property="IsDefault" Value="True">
                    <Setter Property="Background" Value="#2196F3"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DialogTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#2196F3"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Instructions -->
        <TextBlock Grid.Row="0" 
                   Text="Enter filter values separated by spaces."
                   TextWrapping="Wrap"
                   Margin="0,0,0,8"
                   FontWeight="Medium"/>

        <TextBlock Grid.Row="1" 
                   Text="Choose how multiple values should be combined:"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"/>

        <!-- Radio buttons -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <RadioButton x:Name="OrRadio"
                         Content="OR (show rows that match ANY value)"
                         IsChecked="True"
                         Margin="0,0,0,6"
                         FontSize="12"/>

            <RadioButton x:Name="AndRadio"
                         Content="AND (show rows that contain ALL values)"
                         FontSize="12"/>
        </StackPanel>

        <!-- Text input -->
        <TextBlock Grid.Row="3" 
                   Text="Filter text:"
                   Margin="0,0,0,4"
                   FontWeight="Medium"/>

        <TextBox x:Name="FilterTextBox"
                 Grid.Row="4"
                 Style="{StaticResource DialogTextBoxStyle}"
                 VerticalAlignment="Top"
                 Height="32"
                 Margin="0,0,0,16"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button x:Name="ApplyButton"
                    Content="Apply Filter"
                    Style="{StaticResource DialogButtonStyle}"
                    IsDefault="True"
                    Click="ApplyButton_Click"/>

            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Style="{StaticResource DialogButtonStyle}"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>