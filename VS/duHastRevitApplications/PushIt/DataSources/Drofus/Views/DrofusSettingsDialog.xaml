<Window x:Class="duHastNet.PushIt.DataSources.Drofus.Views.DrofusSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:duHastNet.PushIt.DataSources.Drofus.ViewModels"
        mc:Ignorable="d"
        Title="drofus Connection Settings"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Width="440"
        d:DataContext="{d:DesignInstance Type=vm:DrofusSettingsDialogViewModel}">

    <Window.Resources>
        <!-- Status message colour: green for success, red for error -->
        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding StatusSeverity}" Value="Error">
                    <Setter Property="Foreground" Value="#CC2200"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding StatusSeverity}" Value="OK">
                    <Setter Property="Foreground" Value="#007700"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Validation error tooltip style (matches rest of project) -->
        <Style x:Key="ValidationTextBoxStyle" TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                            Value="{Binding (Validation.Errors)[0].ErrorContent,
                                   RelativeSource={RelativeSource Self}}"/>
                    <Setter Property="BorderBrush" Value="#CC2200"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="Auto"/>  <!-- Form -->
            <RowDefinition Height="Auto"/>  <!-- Status -->
            <RowDefinition Height="Auto"/>  <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- ═══ Header ════════════════════════════════════════════════════════ -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="drofus API Connection"
                       FontWeight="SemiBold"
                       FontSize="13"/>
            <TextBlock TextWrapping="Wrap"
                       Foreground="#555"
                       FontSize="11"
                       Margin="0,4,0,0"
                       Text="Enter your drofus connection details. Generate an API-key in drofus under&#x0a;Power Query → Credentials → Generate Key."/>
        </StackPanel>

        <!-- ═══ Form ══════════════════════════════════════════════════════════ -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Region -->
            <TextBlock Grid.Row="0" Grid.Column="0"
                       Text="Region"
                       VerticalAlignment="Center"
                       Margin="0,0,8,8"/>
            <ComboBox Grid.Row="0" Grid.Column="1"
                      Margin="0,0,0,8"
                      ItemsSource="{Binding AvailableRegions}"
                      SelectedItem="{Binding SelectedRegion}"
                      IsEnabled="{Binding IsValidating, Converter={StaticResource InverseBoolConverter}}"/>

            <!-- Database ID -->
            <TextBlock Grid.Row="1" Grid.Column="0"
                       Text="Database ID"
                       VerticalAlignment="Center"
                       Margin="0,0,8,8"/>
            <TextBox Grid.Row="1" Grid.Column="1"
                     Margin="0,0,0,8"
                     Style="{StaticResource ValidationTextBoxStyle}"
                     Text="{Binding DatabaseId, UpdateSourceTrigger=PropertyChanged,
                            ValidatesOnNotifyDataErrors=True}"
                     IsEnabled="{Binding IsValidating, Converter={StaticResource InverseBoolConverter}}"
                     ToolTip="The database identifier from your drofus project URL (e.g. my-project-db)"/>

            <!-- Project Number -->
            <TextBlock Grid.Row="2" Grid.Column="0"
                       Text="Project Number"
                       VerticalAlignment="Center"
                       Margin="0,0,8,8"/>
            <TextBox Grid.Row="2" Grid.Column="1"
                     Margin="0,0,0,8"
                     Style="{StaticResource ValidationTextBoxStyle}"
                     Text="{Binding ProjectNumber, UpdateSourceTrigger=PropertyChanged,
                            ValidatesOnNotifyDataErrors=True}"
                     IsEnabled="{Binding IsValidating, Converter={StaticResource InverseBoolConverter}}"
                     ToolTip="The project number within the database (e.g. 01)"/>

            <!-- API Key -->
            <TextBlock Grid.Row="3" Grid.Column="0"
                       Text="API Key"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"/>
            <TextBox Grid.Row="3" Grid.Column="1"
                     Style="{StaticResource ValidationTextBoxStyle}"
                     Text="{Binding ApiKey, UpdateSourceTrigger=PropertyChanged,
                            ValidatesOnNotifyDataErrors=True}"
                     IsEnabled="{Binding IsValidating, Converter={StaticResource InverseBoolConverter}}"
                     FontFamily="Consolas"
                     FontSize="11"
                     ToolTip="Paste your drofus API-key here"/>
        </Grid>

        <!-- ═══ Status message ════════════════════════════════════════════════ -->
        <TextBlock Grid.Row="2"
                   Style="{StaticResource StatusTextStyle}"
                   Text="{Binding StatusMessage}"
                   Visibility="{Binding StatusMessage,
                                Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- ═══ Buttons ═══════════════════════════════════════════════════════ -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Busy indicator -->
            <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Visibility="{Binding IsValidating,
                                     Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="Connecting…"
                           Foreground="#555"
                           FontSize="11"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Content="OK"
                    Width="80"
                    IsDefault="True"
                    Command="{Binding OkCommand}"/>

            <Button Grid.Column="3"
                    Content="Cancel"
                    Width="80"
                    IsCancel="True"
                    Command="{Binding CancelCommand}"/>
        </Grid>
    </Grid>
</Window>
