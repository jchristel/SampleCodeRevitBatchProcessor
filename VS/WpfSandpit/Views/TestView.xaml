<UserControl x:Class="WpfSandpit.Views.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfSandpit.Views"
             xmlns:utilsViews ="clr-namespace:duHastNet.Utils.WPF.Views;assembly=Utils.23.0.0.3"
             xmlns:converters="clr-namespace:duHastNet.Utils.WPF.Converters;assembly=Utils.23.0.0.3"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="800">
    <UserControl.Resources>
        <!--boolean visibility converter-->
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="10 10 10 10">
        <!-- Add your UI elements here -->
        <Grid.RowDefinitions>
            <!-- header row -->
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <!-- header row content -->
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <!-- header row -->
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Text="duHastNet.PushIt..Real Good(tm)" FontSize="24"/>
            <ToggleButton
                Grid.Column="1" Grid.Row="0"
                x:Name = "toggleSafety" 
                Content = "{Binding  SafetyOffButtonText}"   
                Margin = "0 10 0 10"
                Width = "108"
                HorizontalAlignment = "Right"
                IsChecked="{Binding SafetyOffMode, Mode=TwoWay}">
            </ToggleButton>
        </Grid>

        <Border Grid.Row="1" CornerRadius="1"  Margin="0 2 0 2">
            <Grid>
                <TextBlock
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Visibility="{Binding IsWaitingForRevitCommandToFinish, Converter={StaticResource BooleanToVisibilityConverter}}"
                    FontSize="24"
                    Text="Updating Revit..."/>
                <ListView 
                    Margin="0 25 0 0" 
                    Visibility="{Binding IsWaitingForRevitCommandToFinish, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                    SelectionMode="Single">
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>
