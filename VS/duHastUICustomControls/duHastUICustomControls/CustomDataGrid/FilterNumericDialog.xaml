<Window x:Class="duHastNet.UI.CustomControls.CustomDataGrid.FilterNumericDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Filter Numeric Column"
        Width="450"
        Height="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="BorderBrush" Value="#2196F3"/>
                </Trigger>
                <Trigger Property="IsDefault" Value="True">
                    <Setter Property="Background" Value="#2196F3"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DialogTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="28"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#2196F3"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Instructions -->
        <TextBlock Grid.Row="0" 
                   Text="Choose filter operation:"
                   FontWeight="Medium"
                   Margin="0,0,0,16"/>

        <!-- Filter operation selection -->
        <ComboBox x:Name="OperationComboBox"
                  Grid.Row="1"
                  Margin="0,0,0,16"
                  Height="32"
                  SelectionChanged="OperationComboBox_SelectionChanged">
            <ComboBoxItem Content="Is equal to" Tag="Equal"/>
            <ComboBoxItem Content="Is greater than" Tag="GreaterThan"/>
            <ComboBoxItem Content="Is greater than or equal to" Tag="GreaterThanOrEqual"/>
            <ComboBoxItem Content="Is less than" Tag="LessThan"/>
            <ComboBoxItem Content="Is less than or equal to" Tag="LessThanOrEqual"/>
            <ComboBoxItem Content="Is in range" Tag="Range"/>
        </ComboBox>

        <!-- Single value input (for most operations) -->
        <Grid x:Name="SingleValuePanel" Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="ValueLabel" 
                       Grid.Column="0"
                       Text="Value:" 
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"/>

            <TextBox x:Name="ValueTextBox"
                     Grid.Column="1"
                     Style="{StaticResource DialogTextBoxStyle}"
                     TextChanged="ValueTextBox_TextChanged"/>
        </Grid>

        <!-- Range input (for range operation) -->
        <Grid x:Name="RangePanel" Grid.Row="2" Margin="0,0,0,12" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0"
                       Text="From:" 
                       VerticalAlignment="Center"
                       Margin="0,0,8,4"/>

            <TextBox x:Name="FromValueTextBox"
                     Grid.Row="0" Grid.Column="1"
                     Style="{StaticResource DialogTextBoxStyle}"
                     Margin="0,0,0,4"
                     TextChanged="RangeTextBox_TextChanged"/>

            <TextBlock Grid.Row="1" Grid.Column="0"
                       Text="To:" 
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"/>

            <TextBox x:Name="ToValueTextBox"
                     Grid.Row="1" Grid.Column="1"
                     Style="{StaticResource DialogTextBoxStyle}"
                     TextChanged="RangeTextBox_TextChanged"/>
        </Grid>

        <!-- Current filter display -->
        <TextBlock x:Name="CurrentFilterText"
                   Grid.Row="3"
                   FontStyle="Italic"
                   Foreground="Gray"
                   Margin="0,0,0,16"
                   TextWrapping="Wrap"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button x:Name="ClearButton"
                    Content="Clear Filter"
                    Style="{StaticResource DialogButtonStyle}"
                    Click="ClearButton_Click"/>

            <Button x:Name="ApplyButton"
                    Content="Apply Filter"
                    Style="{StaticResource DialogButtonStyle}"
                    IsDefault="True"
                    Click="ApplyButton_Click"/>

            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Style="{StaticResource DialogButtonStyle}"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>