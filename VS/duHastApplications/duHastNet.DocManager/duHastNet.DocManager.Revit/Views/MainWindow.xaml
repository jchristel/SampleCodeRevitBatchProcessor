<Window x:Class="duHastNet.DocManager.Revit.Views.MainWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:duHastNet.DocManager.Revit"
             xmlns:views="clr-namespace:duHastNet.DocManager.Revit.Views"
             xmlns:vms="clr-namespace:duHastNet.DocManager.Revit.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Title="Document Manager — Revit Integration" Height="600" Width="1000"
             FontSize="14">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type vms:RevitIntegrationViewModel}">
                <views:RevitIntegrationView/>
            </DataTemplate>
            <!-- POC view kept for future reference -->
            <DataTemplate DataType="{x:Type vms:PyRevitDocumentListViewModel}">
                <views:PyRevitDocumentListView/>
            </DataTemplate>
        </Grid.Resources>
        <ContentControl Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
