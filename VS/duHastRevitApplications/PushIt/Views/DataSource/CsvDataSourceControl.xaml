<UserControl x:Class="duHastNet.PushIt.Views.DataSource.CsvDataSourceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="600">

    <!--
        DataContext: CsvDataSourceControlViewModel
        Displayed by DataSourceControl when DataSourceType.Csv is selected.
        Replaces the inline file-picker row that was previously in RoomsSelection.xaml.
    -->

    <UserControl.Resources>
        <!-- Validation error style matching RoomsSelection.xaml convention -->
        <Style TargetType="TextBox">
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <AdornedElementPlaceholder/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <StackPanel>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                            <ItemsControl Margin="0 1 0 0" FontSize="12"
                                          ItemsSource="{TemplateBinding Validation.Errors}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ErrorContent}" Foreground="Red"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>   <!-- label -->
            <ColumnDefinition Width="*"/>      <!-- file path text box -->
            <ColumnDefinition Width="Auto"/>   <!-- Browse button -->
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0"
                   Margin="0 0 8 0"
                   VerticalAlignment="Center"
                   Text="Path to SoA:"/>

        <TextBox Grid.Column="1"
                 Margin="0 0 5 0"
                 VerticalAlignment="Center"
                 Text="{Binding FilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <Button Grid.Column="2"
                Padding="16 4"
                VerticalAlignment="Center"
                Content="Browse"
                Command="{Binding BrowseCommand}"/>
    </Grid>
</UserControl>
