<UserControl x:Class="duHastNet.DocManager.UI.Shared.Views.Transmittal.TransmittalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:utilsViews="clr-namespace:duHastNet.Utils.WPF.Views;assembly=Utils.25.0.1.1"
             xmlns:viewmodels="clr-namespace:duHastNet.DocManager.UI.Shared.ViewModels.Transmittal"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=viewmodels:TransmittalViewModel}">

    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <!-- header banner -->
            <RowDefinition Height="Auto"/>
            <!-- global message banner -->
            <RowDefinition Height="Auto"/>
            <!-- revisions section -->
            <RowDefinition Height="2*"/>
            <!-- documents section -->
            <RowDefinition Height="3*"/>
            <!-- action bar -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- Row 0 — Header Banner                                   -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <Border Grid.Row="0"
                Background="#2196F3"
                Padding="15,10"
                Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Document Manager"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="White"/>
                <TextBlock Text="Transmittal — Revision History"
                           FontSize="12"
                           Foreground="White"
                           Opacity="0.9"/>
            </StackPanel>
        </Border>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- Row 1 — Global Message Banner                           -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <Grid Grid.Row="1">
            <utilsViews:GlobalMessageView DataContext="{Binding GlobalMessageViewModel}"/>
        </Grid>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- Row 2 — Revisions Section                               -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <Border Grid.Row="2"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="3"
                Margin="0,0,0,6">
            <Grid>
                <Grid.RowDefinitions>
                    <!-- section header -->
                    <RowDefinition Height="Auto"/>
                    <!-- status message -->
                    <RowDefinition Height="Auto"/>
                    <!-- placeholder / future datagrid -->
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- section header -->
                <TextBlock Grid.Row="0"
                           Text="Revisions"
                           FontWeight="SemiBold"
                           FontSize="13"
                           Margin="8,6,8,4"/>

                <!-- status message — collapses when empty -->
                <TextBlock Grid.Row="1"
                           Text="{Binding RevisionsStatusMessage}"
                           FontSize="12"
                           Foreground="#555555"
                           Margin="8,4,8,4"
                           TextWrapping="Wrap">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- placeholder for revisions DataGrid -->
                <Border Grid.Row="2"
                        Background="#F5F5F5"
                        BorderBrush="#CCCCCC"
                        BorderThickness="1"
                        Margin="8,4,8,8">
                    <TextBlock Text="[Revisions DataGrid — placeholder]"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="#AAAAAA"
                               FontStyle="Italic"
                               FontSize="12"/>
                </Border>
            </Grid>
        </Border>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- Row 3 — Documents Section                               -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <Border Grid.Row="3"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="3"
                Margin="0,0,0,6">
            <Grid>
                <Grid.RowDefinitions>
                    <!-- section header + mode toggle -->
                    <RowDefinition Height="Auto"/>
                    <!-- options row -->
                    <RowDefinition Height="Auto"/>
                    <!-- status message -->
                    <RowDefinition Height="Auto"/>
                    <!-- placeholder / future datagrid -->
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- section header + mode toggle -->
                <Grid Grid.Row="0" Margin="8,6,8,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Documents"
                               FontWeight="SemiBold"
                               FontSize="13"
                               VerticalAlignment="Center"/>

                    <!-- mode toggle placeholder -->
                    <StackPanel Grid.Column="2"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <RadioButton Content="Show All"
                                     GroupName="DocumentMode"
                                     IsChecked="True"
                                     Margin="0,0,10,0"
                                     VerticalContentAlignment="Center"/>
                        <RadioButton Content="By Revisions Only"
                                     GroupName="DocumentMode"
                                     VerticalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <!-- options row -->
                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            Margin="8,0,8,6">
                    <CheckBox Content="Show inactive documents"
                              Margin="0,0,20,0"
                              VerticalContentAlignment="Center"/>
                    <CheckBox Content="Include document history"
                              VerticalContentAlignment="Center"/>
                </StackPanel>

                <!-- status message — collapses when empty -->
                <TextBlock Grid.Row="2"
                           Text="{Binding DocumentsStatusMessage}"
                           FontSize="12"
                           Foreground="#555555"
                           Margin="8,0,8,4"
                           TextWrapping="Wrap">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- placeholder for documents DataGrid -->
                <Border Grid.Row="3"
                        Background="#F5F5F5"
                        BorderBrush="#CCCCCC"
                        BorderThickness="1"
                        Margin="8,0,8,8">
                    <TextBlock Text="[Documents DataGrid — placeholder]"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="#AAAAAA"
                               FontStyle="Italic"
                               FontSize="12"/>
                </Border>
            </Grid>
        </Border>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!-- Row 4 — Action Bar                                      -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <Border Grid.Row="4"
                Background="#F5F5F5"
                BorderBrush="#CCCCCC"
                BorderThickness="0,1,0,0"
                Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- left — export status hint -->
                <TextBlock Grid.Column="0"
                           Text="{Binding ExportStatusMessage}"
                           VerticalAlignment="Center"
                           FontSize="12"
                           Foreground="#555555"/>

                <!-- export button -->
                <Button Grid.Column="1"
                        Content="Export to CSV"
                        Command="{Binding ExportToCsvCommand}"
                        FontWeight="SemiBold"
                        Foreground="White"
                        Background="#2196F3"
                        BorderThickness="0"
                        Padding="14,7"
                        Margin="0,0,8,0"/>

                <!-- close button -->
                <Button Grid.Column="2"
                        Content="Close"
                        Command="{Binding CloseCommand}"
                        Padding="14,7"/>
            </Grid>
        </Border>

    </Grid>
</UserControl>
