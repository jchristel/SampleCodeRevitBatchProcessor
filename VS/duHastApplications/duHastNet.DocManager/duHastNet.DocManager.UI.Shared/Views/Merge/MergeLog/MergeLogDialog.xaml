<Window x:Class="duHastNet.DocManager.UI.Shared.Views.Merge.MergeLog.MergeLogDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:duHastNet.DocManager.UI.Shared.Views"
        xmlns:vm="clr-namespace:duHastNet.DocManager.UI.Shared.ViewModels.Merge.MergeLog"
        mc:Ignorable="d"
        Title="{Binding DialogTitle}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinWidth="600"
        MinHeight="400"
        Width="800"
        Height="600"
        d:DataContext="{d:DesignInstance Type=vm:MergeLogDialogViewModel}">

    <Window.Resources>
        <!-- Style for document headers -->
        <Style x:Key="DocumentHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,5,0,2"/>
        </Style>

        <!-- Style for message text -->
        <Style x:Key="MessageTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="20,2,0,2"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Summary Header -->
        <Border Grid.Row="0"
                Background="#F5F5F5"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="3"
                Padding="10"
                Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Merge Operation Complete"
                           FontWeight="Bold"
                           FontSize="14"
                           Margin="0,0,0,5"/>
                <TextBlock Text="{Binding SummaryMessage}"
                           FontSize="12"/>
            </StackPanel>
        </Border>

        <!-- Column Headers -->
        <Border Grid.Row="1"
                Background="#E8E8E8"
                BorderBrush="Gray"
                BorderThickness="1"
                Padding="5"
                Margin="0,0,0,5">
            <TextBlock Text="Process Messages (Errors shown in red)"
                       FontWeight="SemiBold"
                       FontSize="11"/>
        </Border>

        <!-- Scrollable Log Content -->
        <Border Grid.Row="2"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="3">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding LogEntries}"
                              Margin="5">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,5,0,10">
                                <!-- Document Header -->
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding DocumentBullet}"
                                               Style="{StaticResource DocumentHeaderStyle}"
                                               Margin="0,5,5,2"/>
                                    <TextBlock Text="{Binding DocumentFileName}"
                                               Style="{StaticResource DocumentHeaderStyle}"/>
                                </StackPanel>

                                <!-- Process Messages -->
                                <ItemsControl ItemsSource="{Binding Messages}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding MessageBullet}"
                                                           Foreground="{Binding MessageColor}"
                                                           Style="{StaticResource MessageTextStyle}"
                                                           Margin="20,2,5,2"/>
                                                <TextBlock Text="{Binding Message}"
                                                           Foreground="{Binding MessageColor}"
                                                           Style="{StaticResource MessageTextStyle}"
                                                           Margin="0,2,0,2"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- OK Button -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="OK"
                    Command="{Binding OkCommand}"
                    Width="80"
                    Height="30"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
