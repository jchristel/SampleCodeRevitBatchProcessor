<Window x:Class="duHastNet.PushIt.Views.MainWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:duHastNet.PushIt"
             xmlns:views="clr-namespace:duHastNet.PushIt.Views"
             xmlns:vms="clr-namespace:duHastNet.PushIt.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
            Title="PushIt" Height="450" Width="800"
            FontSize="14">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type vms:RoomsMainViewModel}">
                <views:RoomsSelection/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:SettingsViewModel}">
                <views:SettingsView/>
            </DataTemplate>
        </Grid.Resources>
        <ContentControl Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
