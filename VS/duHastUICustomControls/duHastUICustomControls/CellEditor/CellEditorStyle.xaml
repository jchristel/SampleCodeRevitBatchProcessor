<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:duHastNet.UI.CustomControls"
                    xmlns:converters="clr-namespace:duHastNet.UI.CustomControls.Converters"
                    xmlns:dg="clr-namespace:duHastNet.UI.CustomControls.CustomDataGrid">

    <Style TargetType="{x:Type local:CellEditor}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CellEditor}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Toolbar -->
                            <Border Grid.Row="0" 
                                    Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                                    BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                                    BorderThickness="0,0,0,1"
                                    Padding="5">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">

                                    <Button x:Name="PART_AddRowButton"
                                            Content="Add Row"
                                            Margin="0,0,5,0"
                                            Padding="8,4"
                                            MinWidth="70">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=local:CellEditor}, Path=AllowAddRow}" Value="False">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <Button x:Name="PART_DuplicateRowButton"
                                            Content="Duplicate Row"
                                            Margin="0,0,5,0"
                                            Padding="8,4"
                                            MinWidth="90">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Setter Property="IsEnabled" Value="False"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=local:CellEditor}, Path=AllowDuplicateRow}" Value="False">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ElementName=PART_DataGrid, Path=SelectedItem, Converter={x:Static converters:NullToBooleanConverter.Instance}}" Value="True">
                                                        <Setter Property="IsEnabled" Value="True"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <Button x:Name="PART_DeleteRowButton"
                                            Content="Delete Row"
                                            Margin="0,0,5,0"
                                            Padding="8,4"
                                            MinWidth="80">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Setter Property="IsEnabled" Value="False"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=local:CellEditor}, Path=AllowDeleteRow}" Value="False">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ElementName=PART_DataGrid, Path=SelectedItem, Converter={x:Static converters:NullToBooleanConverter.Instance}}" Value="True">
                                                        <Setter Property="IsEnabled" Value="True"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                </StackPanel>
                            </Border>

                            <!-- DataGrid -->
                            <dg:DynamicDataGrid x:Name="PART_DataGrid"
                                              Grid.Row="1"
                                              dg:DataGridColumnHeaderBehavior.EnableColumnManagement="True"
                                              CanUserReorderColumns="True"
                                              CanUserSortColumns="True"
                                              CanUserResizeColumns="True"
                                              SelectionMode="Single"
                                              GridLinesVisibility="All"
                                              AlternatingRowBackground="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"
                                              HeadersVisibility="Column">

                                <!-- Context Menu for right-click operations -->
                                <dg:DynamicDataGrid.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem x:Name="PART_AddRowMenuItem" Header="Add Row"/>
                                        <MenuItem x:Name="PART_DuplicateRowMenuItem" Header="Duplicate Row"/>
                                        <MenuItem x:Name="PART_DeleteRowMenuItem" Header="Delete Row"/>
                                    </ContextMenu>
                                </dg:DynamicDataGrid.ContextMenu>
                            </dg:DynamicDataGrid>

                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>